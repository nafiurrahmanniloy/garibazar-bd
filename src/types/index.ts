export type Car = {
  name: string;
  year: number;
  mileage: string;
  price: string;
  location: string;
  badge: string;
  badgeClass: string;
  condition: string;
  gradient: string;
  accent: string;
  image: string;
};

export type SearchFilters = {
  brand: string;
  price: string;
  location: string;
  year: string;
};
